# チューナーエージェント

**電波を掴むところ。** ここだけが機材に触ります。**なぜこの形なのか**と、
`bun` から `.NET` に落ち着くまでに測ったことを置いてあります。

| 探しもの | 見る場所 |
| --- | --- |
| 全体像 | [architecture.md](architecture.md) |
| 番組表・スキャン・持ち物 | [data.md](data.md) |
| ファイル・環境変数・画面 | [app.md](app.md) |

**機材に触るのはここだけです。** denpa から触れないものが3つあり、どれも
チューナーを持つコンテナでしか出来ません。

- **チューナーデバイス** — `/dev/dvb/*` が見えているのはあちらだけ
- **B-CASカード** — pcscd 経由でしか読めず、その pcscd はあちらにしか居ません
- **選局そのもの** — デバイスを ioctl で直接掴みます (`recisdb` は像から外しました。
  [下記](#net-で書き直すまで-経緯と実測))

持っている仕事は3つ。

- **選局して TS を流す**
- **優先度つきの取り合い** (誰が何本掴んでいるか。低いものを切って高いものを通す)
- **どのチャンネルが使えるかの控え** (スキャンの結果を預かって配る。**中身は作らない**)

**TS は1バイトも解釈しません。** NIT も SDT も EIT も解かないので、
`src/lib/ts` に依存していません。総当たりのスキャンでさえ、やることは
「言われた物理チャンネルを順に開いて流す」だけです。

## なぜ denpa が全部持つのか

**局も番組表も時刻も、覚えているのは denpa だけ**です。同じものを2箇所が持つと
「あちらがまだ知らない局は、こちらからは増やせない」という縛りが必ず出ます。

持っているぶん、こちらで決められます。

- **番組表は、チューナーが空いているだけ並列に集める。** 急がせたいときに
  何かを再起動する必要が無い ([data.md](data.md#番組表の集め方))
- **録画中でもスキャンできる。** 止まるものが1つも無い
- **優先度は素直な数のまま。** 下限も、特別扱いされる値も無い ([下記](#取り合いの決まり))

## 取り合いの決まり

優先度は**大きいほうが勝ち**、下限がありません。番号は denpa が決めます
(`config.priority`)。

| | 値 | |
| --- | --- | --- |
| 録画 | 10 | 放送は二度と来ないので、ここだけは譲らせない |
| ライブ視聴 | 9 | 画面の前で人が待っている。録画にだけ譲る ([stream.md](stream.md)) |
| 番組表 (今すぐ) | 8 | 人が押して待っている番組表集め。**録画以外からは全部取り上げる** |
| チャンネルスキャン | 5 | 人が押して待っている。番組表には譲らせるが、録画中のチューナーは使わない |
| 番組表 | 3 | |
| 局ロゴ | 1 | ロゴが出なくても番組表は読めるが、逆は成り立たない |

探す順番は4段。

1. **同じチャンネルに相乗り** — 番組表・ロゴ・録画が同じ物理チャンネルなら、選局は1本で足ります
2. **畳むのを待っているだけの本** — 誰も読んでいないのに余韻 (5秒) で残っている選局。
   **1人の視聴で2冊塞がないため**に、空いている本より先に使い回します
3. 空いているチューナー
4. 自分より弱い相手から取り上げる

**2段目は取引です。** ライブ視聴でチャンネルを変えると、以前は別のアダプタへ移って
いました (前のアダプタは余韻の間そのチャンネルのまま残るので、**直前のチャンネルへ
戻るのが 4ms**)。1人で2本塞ぐのをやめた代わりに、戻りも選局し直しになります
(実測 802ms)。**切り替えそのものは速くなりません** — どちらの道でも同じだけ
電波の同期を待つためです。

2段目は取りこぼすことがあります。切った直後に次を頼むと、こちらが切断に気付く前に
要求が届いて `Sinks` がまだ 0 でないためです。**そのときは3段目に落ちるだけ**なので、
映らなくなることはありません。

## 相乗りの決まり

**チューナーは4本しかありません。** 電波から読みたいものは4つ (番組表・局ロゴ・
CM検出用ロゴ・録画) あって、どれも「その物理チャンネルを開く」ところは同じです。
**同じチャンネルなら1本で足りる**ので、乗れるものは全部乗せます。

エージェント側は1回の選局 (`Lease`) に読み手 (`Sink`) を何人でもぶら下げます。
**B25 の復号は選局につき1回**で、そこから先を人数ぶん配るだけです
(MULTI2 は TS 全部に掛かるので、読み手ごとに解くと台無しになります)。

| 誰が | 強さ | 乗り方 | 上限 |
| --- | --- | --- | --- |
| **録画** | 10 | 必ず自分で開く。**乗せる側** | 番組の終わりまで |
| **番組表** | 3 (押されている間は 8) | 開く。同じチャンネルが開いていれば只 | 1チャンネル10分 |
| **局ロゴ (PNG)** | 1 | **開いている選局にだけ**乗る (`logo.ride`) | 3分 |
| 〃 (自分から取りに行くとき) | 1 | 開く | 地上波6分 / 衛星20分 |
| **CM検出用ロゴ (`.lgd`)** | 1 | 開いていれば乗る。無ければ空きがあるときだけ | 5分 |
| **チャンネルスキャン** | 5 | 開く (総当たりなので乗れない) | 1チャンネル30秒 |

**いちばん効くのは録画です。** 録画は物理チャンネルを丸ごと開いているので、
そこに流れている EIT も CDT も只で読めます。実際に取っているのは番組表で、
**同じ流れから読んで5分ごとに書き出します** — 開き直しすら要りません。
実機で確認: 録画中の T19 と T21 は、番組表の更新が常に1分以内でした。
そのぶん番組表集めはそのチャンネルへ**6時間行きません** (行ったばかりと分かるため)。

**ロゴは録画の流れからは拾いません。** 衛星のカルーセルは数百KBあり、録画の
一本道に混ぜたくないためです。代わりに `tuners` の知らせを合図に**同じチャンネルを
別に開いて**乗ります。チューナーは増えませんが、**TS がもう一度 HTTP を渡ります**
— 増える資源はそこだけで、足りないのはチューナーのほうなので、この取引で正しい。

**只で乗れるものから先に回します** (CM検出用ロゴの `ridingFirst`)。1局に5分かかるので、
順番を間違えると乗るはずだった録画が終わってしまいます。

**番組表だけは「開いているものを先に」しません。** あちらは**薄い局から先に**行く
のがもっと大事で (入れた直後は全部空)、順番の理由が2つあるとどちらも中途半端に
なります。同じチャンネルが開いていれば結局は只で乗るので、損はしていません。

## 選局は、読み手が切るまで終わらない

**向こうから終わったなら、それは失敗です。** チューナーを取り上げられた・`recisdb` が落ちた・
デバイスが黙った、のいずれかであって、「録り終えた」ではありません。

エージェントは取り上げた相手のストリームを**接続ごと壊して**畳みます (`http.Abort()`)。
きれいに閉じると、受け取る側には正常終了として届くためです。bun 版は接続を壊せず、
そのせいで**取り上げられた録画が尻切れのまま「録れた」ことになっていました**
([下記](#書き直しにはなりませんでした))。

**壊すのは、送り始めたあとだけです。** 1バイトも送らないうちに落ちたなら状態行も
まだなので、**理由を付けて 500 で返します**。どの落ち方も接続を壊していた頃は、
番組表集めが同じ数チャンネルを毎周落とし続けているのに、記録に残るのは
`socket connection was closed unexpectedly` だけで、**電波なのか掴み損ねなのか
設定なのかが分かりませんでした**。

**環 (リングバッファ) が溢れても畳みません。** `read` が `EOVERFLOW` を返すのは
「読むのが追いつかず、ドライバが捨てた」であって終わりではなく、読み続ければ
続きが来ます。これを終わりとして扱っていた頃は、番組表を4本まとめて開いた直後に
1〜2本が落ちていました。溢れた回数はログに残します。

**溢れるのは dvr の環です。** 全PIDを `DMX_OUT_TS_TAP` で流すので、中身は demux の
フィルタではなく **dvr の環**に積まれます。`DMX_SET_BUFFER_SIZE` を demux にだけ
言っていた頃は、**大きくしたつもりのものが使われていませんでした** — 実際に使われる
のはカーネルの既定 (10 × 188 × 1024 ≒ 1.8MB) で、地上波の 18Mbit/秒 では
**0.9 秒ぶん**しかありません。読む側が GC などで一瞬止まるだけで溢れます。

実機の記録では**24時間で 249 回**。アダプタ4本すべて・チャンネルもばらばら・時間帯も
均等 (毎時 5〜17回) だったので、特定の相手ではなく溜めが浅いことのほうが疑わしい、
という読みでした。いまは dvr にも 8MB を言っています (3.5 秒ぶん。4本でも 32MB)。
**断られても選局は続けます** — 広げられないことは、選局を落とす理由にはなりません。

**広げたあとで測り直したら、24時間で 302 回でした。** 減っていません
(毎時 5〜28回で、やはり均等)。
**回数だけでは、溜めが浅いのか読み手が止まっているのかを分けられません。**
そこで、溢れたときに**読み手がどれだけ空いていたか**も出すようにしました。

最初に出た2件はどちらも **4.1秒・4.3秒**で、しかも**局を変えた直後**でした。
デバイスは選局を跨いで開きっぱなしなので、**局を変えている間は誰も読んで
いません** — その間もドライバは電波を積むので、読み始めた最初の1回が必ず
溢れとして返り、「空いた時間」は選局に掛かった時間そのものになります。
**読み手が遅かったのではなく、読み手が居なかった**わけです。
いまは読み始めるところで測り直し (`DeviceStream.Begin`)、**選局の前に埋まって
いたぶんは数に入れず**「選局前に環が N 回溢れていました」と別に書きます —
黙って捨てると「本当に落ちていないのか」が分からなくなるためです。

**測り直した結果、50分で1回・空いた時間 0.0秒** — それも選局を跨いで残っていた
印が最初の `read` で返っただけでした。つまり **249回も302回も、ほとんどが
選局のたびに1回ずつ増えていたぶん**で、読み手が追いつかなかったわけでは
ありませんでした。

環は 3.5 秒ぶんなので、

| 空いた時間 | 読み |
| --- | --- |
| **1秒あたりで頭打ち** | 広げたはずの溜めが効いていない (既定の 1.8MB = 0.9秒ぶんのまま) |
| **3.5秒を超える** | 溜めの深さではなく、**読み手が止まる理由**が本題。疑わしいのは読む輪の中で同期に回している B25 の復号 (`AribB25.cs`)・GC・CPU の取り合い |

**それでも約束は理由ではなく「終わったという事実」に置きます。** 壊れた接続は
読む側に例外として届きますが、届く保証があるのは*終わったこと*だけです
(電源やネットワークで切れることもあります)。読む側 (denpa) は EOF も例外も
録り終えたと読まず、**どちらも同じように掴み直して**終了時刻まで録り続けます
(`recorder.ts`)。この形なら、エージェントを何語で書いても成り立ちます。

**取り上げた相手が読み終わるまで、次の選局を始めません。** 復号器 (`libaribb25`) は
チューナー1本につき1つで、選局を跨いで持ち回っています。**あれはスレッドセーフでは
ないので**、前の読み手がまだ回っているうちに次を始めると、同じ実体を2本の流れから
叩くことになり、**プロセスごと落ちます** — 実機で `double free or corruption (out)`
(SIGSEGV) が出ました。エージェントが落ちると、そのとき走っている録画は全部死にます。

窓が開いていたのは読み口のほうでした。デバイスは選局を跨いで開いたままなので、
**読み手のほうから降りる道が要ります**。無いと、電波が来ていない間 `read` は
0.2秒ごとに起きて掛け直すだけで永久に戻らず、取り上げられた読み手が居座ります。
いまは降りる合図を読み口まで渡していて (`DeviceStream.Read`)、待つのは取り合いの
錠を放してからです (握ったまま待つと、その間どの本も開けません)。

念のため**復号器そのものも順番に通します**。窓が開いても壊れないほうがよく、
待たされるのは MULTI2 の復号1回ぶんです。二重に入りかけた回数は記録に出します —
静かに直しただけでは、直ったのか元々起きていなかったのかが分かりません。

**解けなくなった復号器は作り直します。** 途中で投げたものは中の解析が半端なところで
止まっているので、`reset` して使い回すと壊れたまま次の選局へ持っていくことになります
(落ちたのは ECM の解析に失敗した直後でした)。デバイスは掴んだままにします —
掴み直すと `Device or resource busy` になります。

**誰も読まなくなってから5秒は掴んだままにします。** 連続した番組を録るときに、
前の録画が離してから次が開くまでの一瞬でデバイスを閉じ直すと、まだ閉じきって
いないところへ開きに行って弾かれます。

**外の選局コマンドはプロセスグループごと終わらせます。** 設定に `command` を書いた
ときの逃げ道の話です。`sh -c` に渡すのがパイプラインだと、sh を殺しても中身
(`recisdb` など) が生き残ってチューナーを掴んだままになり、次のチャンネルが
「デバイスが使用中」で失敗し続けます。

## 設定は2つのファイルに分ける

PVC に置きます (`/app-config`)。

| ファイル | 中身 |
| --- | --- |
| `tuners.json` | 繋いである機材。**denpa の画面から書き換えます** (`PUT /denpa/tuners`) |
| `channels.json` | スキャンで見つかったチャンネルと局 (`PUT /denpa/channels`) |

**どちらも denpa が書き、どちらも JSON です。** YAML をやめたのは、人が手で書く
前提が消えたからです。手書き前提だとコメントを守る必要があり、そのために
「`channels:` の段落だけを差し替える」ようなテキスト操作や、AOT で使える小さな
YAML 読みを自前で持つことになっていました。画面から書き換えるなら、丸ごと
書き直せて壊れにくいほうがいい。

**分けてあるのは、片方を書き換えるときにもう片方を巻き添えにしないため**です。
1つのファイルに機材の定義とスキャン結果を同居させると、スキャンのたびに手で
書いたほうが飛ぶ危険が出ます。

## 選局コマンドは画面から渡させない

設定に持つのは**デバイスと種別と LNB** だけで、選局コマンドはエージェントが
組み立てます。自由な文字列を画面から受け取れるようにすると、**denpa に入れた人が
チューナー側で好きなコマンドを走らせられる**ことになります (しかもあちらは
`privileged`)。

`command` は逃げ道として残してありますが、**設定ファイルに直に書いたときだけ**
効きます。画面には読み取り専用で出るだけです。掴んだまま選局するようになれば
コマンド自体が要らなくなるので、ここは短い付き合いです。

**チューナーは書かなくても動きます。** 定義が無ければ `/dev/dvb/*` を開いて
`DTV_ENUM_DELSYS` で受けられる方式を聞き、地上波か衛星かまで判別します
(実機の PT3 で adapter0/2 が ISDB-S、adapter1/3 が ISDB-T と出ています)。
sysfs にも `recisdb` にもこれを答える口は無く、ioctl を直に叩ける .NET 版だけの
芸当です。

**チューナーを開くときは用途を名乗ります。** チューナー一覧に出るのは掴んでいる
相手の印だけなので、何も渡さないと誰が居座っているのか画面から読めません。
`rec 12` `logo GR/T16` `epg BS15_0` `scan T21` と短く渡して、読める言葉に直すのは
チューナー画面のほうでやります (番組名は DB から引き直します)。

**その名前は、環が溢れたときの記録にも出ます。** 溢れっぱなしなら読む側が
遅いということで、相手が番組表集めなのか観ている人なのかで次に見るところが
変わります。**名乗るのは「その間に居た者」です** — 報告は1分に1回なので、
報告した瞬間の読み手だけを見ていると、**溢れた本人が抜けたあとに報告が回って
「読み手なし」になり**ます。実機の24時間で **302回のうち128回 (42%)** が
それで、多くは直後の報告に同じ局・同じチューナーで `live` が付いていました。

## .NET で書き直すまで (経緯と実測)

局と番組表を読むのも、チャンネルを選り分けるのも denpa の仕事で、チューナー側に
残っているのは「掴む・裁く・流す」だけです ([上記](#なぜ-denpa-が全部持つのか))。

**エージェントは .NET になりました** (`agent/`)。Native AOT で実行ファイル1個、
依存はありません。**配備にも出ています** — 実機で録れて落ち着いたので、bun 版は
消しました。実装が2つあった頃に書いた適合テストはそのまま残してあり
(`agent/conformance.test.ts`)、いまも本物を起こして HTTP の口に当てています。

**`recisdb` は外しました。** 選局のたびに `recisdb` を起こすのをやめて、既定で
自分でデバイスを掴んで解きます (`Tuning.cs` / `ChannelTable.cs` / `AribB25.cs`)。
像からも `recisdb` の deb が消えました (残っているのは `libaribb25` だけ)。
外から見た口だけは同じで、そこは適合テストが押さえています。実機で HTTP 越しに
確かめた数字は、3秒で 7.7 MB、**40960 パケットのうち同期でないもの 0**。

.NET にして1つ増えたのが**チューナーの自動判別**です
([上記](#選局コマンドは画面から渡させない))。sysfs にも `recisdb` にもこれを
答える口は無く、**bun のままでは書けなかったところ**です。

外のコマンドを起こす道は**設定に `command` を書いたときだけ**残しました。
変わった機材のための逃げ道で、偽の選局コマンドで走る適合テストもここを通ります。

**実機で1本録りました。** denpa と同じ道 (HTTP で選局して読む) で 60 秒、
地上波 T27 を録って中身を見ています。

| 見たところ | |
| --- | --- |
| 大きさ | 139.4 MB (18.6 Mbps) |
| パケット | 741375 |
| 同期でないもの | **0** |
| 掛かったまま | **27 (0.004%)** — 最初の ECM で鍵が決まるまでの分 |
| 連続性の抜け | **2** (NULL を除く。選局した直後のもの) |
| PID | 0x0100 映像 / 0x0110 音声 / PAT 645・NIT 64・SDT 32・EIT 2792・TOT 13 |

**denpa 自身の目でも読めました** (`src/lib/ts`)。局は「NHK総合1・東京」
「NHK総合2・東京」「NHK携帯G・東京」、リモコン番号 1、TSID 32736。
17 秒ぶんから番組が 531 件、`みんなの体操[解][字]` のように ARIB の文字も
読めています。**選局からスクランブル解除まで、通しで本物が出ています。**

このとき1つ確かめられたことがあります。**カードが開けなくても選局は続きます。**
上の試しでは libaribb25 が使えず (ホストに `libpcsclite` が無い)、
掛かったまま流れましたが、**止まらずに録れて**、チューナー画面には
「解けません」と理由が出ました。電波は二度と戻ってこないので、
掛かったままでも録っておくほうがましです。

### 録ってみて見つかったこと: **延長追従が効かないかもしれない**

実機の TS を denpa の目で読んだとき、**「いま流れている番組」が1件も取れません
でした。** EIT の p/f は来ています (17 秒で 74 節) が、**running_status が全部 0
(未定義)** でした。denpa は `running_status === 4` (放送中) だけを見て
「いま流れている番組」に入れるので、この局では1件も入りません
(`src/lib/ts/eit.ts`)。

録画の**延長追従はここを見ています**。実際の放送に当てて初めて出たもので、
偽の TS では踏めませんでした (こちらは 4 を入れて作っている)。

**直しました。** `running_status` が 0 のときは**節の番号で決めます** — p/f は
仕様で **section 0 が現在・section 1 が次**と決まっていて、こちらは局の都合で
変わりません。1 (まもなく) や 2 (停止中) は今までどおり入れません。

### 掴んだままチャンネルを変える

実機のチューナーは PT3 で、カーネルの `earth_pt3` が**標準の Linux DVB (v5)** として
出しています (`/dev/dvb/adapterN/{frontend0,demux0,dvr0}`。chardev のデバイスは
1つもありません)。DVB API は**開いたまま `FE_SET_PROPERTY` で選局し直せる**ので、
チャンネルを変えるのに閉じる必要がありません。

ところが `recisdb` は**1回1チャンネルの使い捨て**です
(`recisdb tune --device … --channel … <出力>`。あとから指示を送る口はありません)。
外のコマンドに任せる作りだと、掴む相手が変わるたびにプロセスごと立て直すことになります。

開けっ放しにすると3つ手に入ります。

1. **掴み手が入れ替わる隙間が無くなる。** いまは「前のが終わってから次が開く」まで
   デバイスが宙に浮きます。そこへ別のものが割り込めますし、`recisdb` が生き残って
   掴んだままになる事故も実際にあります (スキャンで**プロセスグループごと**殺して
   いるのはこのためです。[data.md](data.md#チャンネルスキャン))
2. **1回の選局を何人でも読める。** 番組表・ロゴ・録画・視聴が同じ物理チャンネルなら
   1本で足ります。エージェントが持つ「誰が何を掴んでいるか」の帳簿と素直に噛み合います
3. ~~**衛星は同じ周波数の中の切り替えが只になるはず。**~~ **外れました。下の実測。**

**買えないものもはっきりさせておきます。同期そのものは短くなりません。**
周波数を変えれば PLL と復調の同期は必ず要ります。消えるのはプロセスの立ち上げと
デバイスを開く時間だけです。

#### 実測 (PT3 / `earth_pt3` / 実機)

掴んだまま、選局だけを繰り返したときの同期までの時間です
(`denpa-agent --tune /dev/dvb/adapter0/frontend0 BS15_0,BS15_1,BS01_0,BS15_0`)。

| 選局 | 同期 | 最初の1バイト | 同期バイト |
| --- | --- | --- | --- |
| BS15_0 (最初) | 229 ms | 108 ms | 100% |
| BS15_1 (**同じ周波数**) | 187 ms | 186 ms | 100% |
| BS01_0 (周波数を跨ぐ) | 198 ms | 139 ms | 100% |
| BS15_0 (戻る) | 188 ms | 104 ms | 100% |
| T27 / T21 (地上波) | 366–377 ms | 142 ms | 100% |

**「同じ中継の中なら只」は成り立ちませんでした。** TSID を変えるだけの
BS15_0→BS15_1 が 187 ms、周波数を跨ぐ BS01_0 が 198 ms で、**差がありません**。
`tc90522` は TSID を書き換えても選局の手順を丸ごとやり直す作りで、PLL を
掛け直さずに済むという読みが外れています。BS の総当たりが 1/4 になる、
という当ては消えました。

残る2つは残ります。**プロセスの立ち上げが消える**のと、**掴み手が入れ替わる
隙間が無くなる**ことです。後者は絵空事ではなくて、この実測をしている最中にも
`recisdb` が読み手の居なくなったあと生き残り、チューナーを 5 分間掴んだままに
なりました (手で殺しました)。掴んだままなら、そもそも誰も掴み直しません。

代わりに背負うもの:

- **B25 の解除を自分でやる。** いまはカードを使って `recisdb` が解いています。
  カードを持つのはエージェントだけ、というのは既定路線ですが、
  `libaribb25` 相当を抱えることになります
- ~~**選局表を自分で持つ。**~~ **持ちました** (`ChannelTable.cs`)。下記
- **口が2つになります** (下記)。ただしどちらも開いたままで済みます

既製の常駐 (tvheadend など) に乗せ替える手もありますが、あちらは番組表も録画も
持っているので、**同じものを2箇所が持つ形に戻るだけ**です。欲しいのは
「開いたまま選局して、TSを配る」薄い層だけです。

#### 選局表

チャンネル名から周波数を出す表です。これまで持っていたのは `recisdb` のほうで、
こちらは `--channel T27` と渡すだけでした。

**数字は `recisdb` から取りました。** 自分で導き直すより、**いま実機で選局できて
いるものと同じ数字**にすることのほうが大事です。

- 地上波 … `473142857 + (ch-13) × 6 MHz` **Hz で**
- BS … `1049480 + 38360 × n` **kHz で** (中継は 38.36 MHz 刻み)
- CS … `1613000 + 40000 × n` **kHz で** (40 MHz 刻み)

単位が2つあるのは DVB API の決まりです (地上波は Hz、衛星は kHz)。取り違えても
ioctl は通り、**ただ同期しない**ので気付きにくいところです。

衛星でもう1つ要るのが **TSID** です。1つの周波数に何本もの TS が相乗りしていて、
復調器は TSID を書いて選り分けます。ところが denpa が言ってくるのは `BS15_0` の
ような**相対番号**なので、ここで直します。引く先は2つあります。

1. **自分の `channels.json`** — スキャン結果には TSID が入っています。**こちらが勝ちます**
2. 焼き込んだ表 — まだ1度もスキャンしていないときだけ

`recisdb` は 2 だけを持っています。BS は再編があるので焼き込んだ表はいつか古くなり、
実際 `recisdb` の表には手元で受かっている `BS13_3` `BS21_3` `BS23_3` がありません。
**スキャンした人のほうが必ず新しい**ので、こちらを先に引きます。

#### 選局は取り合いの錠の外でやる

掴んだままにした最初の版は、**チューナーの取り合いの錠を握ったまま選局していました。**
電波が来ていないチャンネルは同期待ちの上限 5 秒を使い切るので、その 5 秒のあいだ
プールに触る道が全部止まります。実機で2つ出ていました。

- **チューナーが2本あっても、総当たりが1本ずつしか進まない。** 片方が選局している
  間、もう片方の要求は錠の前で待たされます。空振りのチャンネルは選局が仕事の
  ほとんどなので、**並べた意味がまるごと消えます**
- **スキャンを始めてもチューナー画面が「空き」のまま動かない。** `/denpa/tuners` も
  同じ錠を取るので、選局が終わるまで答えが返りません

錠の中でやるのは**本を押さえるところまで**にして、選局はその外へ出しました
(`TunerPool.Open`)。押さえた印として**読み手を先に入れてから錠を放します** —
入れないまま放すと、まだ選局している最中の本が「誰も読んでいない」と見えて、
次の要求に持っていかれます。実体の入れ物には別の錠を、選局し直す間の順番は
**本ごとの錠**で守ります (別の本は待たない)。

### chardev も開いたままにできる。ただし DVB では代われない

**「開いたまま選局する」は chardev でも成り立ちます。** `px4_drv` の chardev は
1つの fd に対して選局と streaming の開始・停止を投げる作りで、
**閉じずに何度でも選局し直せます**。

```c
#define PTX_SET_CHANNEL     _IOW(0x8d, 0x01, struct ptx_freq)
#define PTX_START_STREAMING _IO(0x8d, 0x02)
#define PTX_STOP_STREAMING  _IO(0x8d, 0x03)

struct ptx_freq { int freq_no; int slot; };
```

`slot` が衛星のスロットなので、**`BS01_0`〜`BS01_3` の切り替えも
`PTX_SET_CHANNEL` 1回**です (DVB の `DTV_STREAM_ID` と同じ考え方)。
むしろ fd が1つで済むぶん DVB より単純で、frontend / demux / dvr を
別々に開く必要がありません。

**では DVB だけにすればいいかというと、それは駄目です。** 「Linux DVB で使える
PLEX のチューナーもある」のは本当ですが、**そうでないものが主力**です。

| 機材 | 口 |
| --- | --- |
| PT2 / PT3 (Earthsoft) | **DVB** (mainline の `earth_pt1` / `earth_pt3`)。実機はこれ |
| PX-S1UD (ISDB-T / USB) | **DVB** (mainline の Siano `smsusb` + `smsdvb`。firmware `isdbt_rio.inp` が要る) |
| PX-BCUD (ISDB-S / USB) | **DVB** (Linux 4.7 以降 mainline) |
| PX4 / PX5 / PX-MLT / e-better 系 | **chardev のみ** (`px4_drv`。作者自身が "not V4L-DVB" と書いています) |

USBの1本ものは DVB に載っていますが、**複数チューナーの PCIe・USB ボードは
`px4_drv` の chardev だけ**です。ここを落とすと「4本挿してある人」がまるごと
外れるので、**2つとも実装します**。

さいわい抽象は同じ形になります — *開く → 選局 → 流し始める → 読む → 選局し直す*。
違うのはその4つをどの ioctl で言うかだけなので、エージェントの中では
**backend を差し替えるだけ**で済みます (`ITuneDevice`)。

**2つとも書きましたが、確かめられたのは DVB だけです。** ここにある機材は PT3 で、
chardev のデバイスは1つもありません。`Px4Tuner` の値は `px4_drv` と `recisdb` の
`character_device.rs` から取ってあり、**実機に当てていません**。

### B25 の解除は `libaribb25`

**これも書けました** (`AribB25.cs`)。実機のカードで確かめてあり、地上波を
掴んだまま解いて **掛かっているパケットが 87.8% → 0.1%** になります (残る 0.1% は
最初の ECM で鍵が決まるまでの分)。

呼び方は関数ポインタの表です。`create_arib_std_b25()` が返す構造体の中身が
そのまま関数ポインタなので、C# からは `delegate* unmanaged` で直に呼べます
(`Marshal.GetDelegateForFunctionPointer` は使いません。遅いうえ AOT で気を遣う)。
**1日ぶんの写経で済む見込み**と書いていたところで、実際そのくらいでした。

**いま実機で動いているものそのものだからです。** `recisdb` は
[recisdb-rs](https://github.com/kazuki0824/recisdb-rs) → `b25-sys` →
[libaribb25](https://github.com/tsukumijima/libaribb25) という積み方で、
このカードとこの電波で解けているのは既にこの実装です。差し替えても
**復号の挙動が変わらない**のが何よりの利点で、他を選ぶ理由が見当たりません。

他の道と、採らない理由:

| 案 | なぜ採らないか |
| --- | --- |
| 元祖の `libarib25` (stz2012) | `libaribb25` はこれの手入れが続いている側。わざわざ古いほうを採る理由が無い |
| MULTI2 と ECM を自分で書く | カードとのやり取り (PC/SC) は書けても、MULTI2 を自前で持つ意味が無い |
| 外部プロセスのまま (いまの `recisdb`) | 呼べはするが、下の「1回だけ解く」と噛み合わない |

設計として押さえておくことが2つあります。

- **解くのは選局ごとに1回** ([相乗りの決まり](#相乗りの決まり))。**エージェントの
  中に持つ**か、外部プロセスに逃がすなら**選局ごとに1つ**にします
- **番組表とロゴには要りません。** PSI/SI (EIT・SDT・NIT・CDT・DSM-CC) は
  スクランブルされていないので、番組表集めもロゴ集めもカード無しで回ります。
  **カードが死んでいても番組表は埋まる**、という切り分けはそのまま残せます

#### カードは1枚でいい。**鍵を配る口を入れました**

想定しているのは「1台に4本挿す」ではなく、**拠点ごとにエージェントとチューナーが
あって、それが何箇所かある**形です。そうなるとカードは1箇所にしかありません。

そこで**カードごと持っていく代わりに、ECM を投げて鍵を貰います**
(`CardShare.cs`)。

```text
拠点B (カード無し)                     拠点A (カードあり)
  選局 → TS ─┬─ MULTI2 で解く          B-CASカード
             └─ ECM 200B ──────────→  proc_ecm
                鍵 16B  ←──────────
```

- **線に乗るのは鍵のやり取りだけ。** ECM は 200 バイトほど、返るのは 16 バイト
- **重いほうは動かしません。** MULTI2 は TS 全部に掛かるので各拠点の手元に残す
- **同じ ECM には同じ鍵を返します** (数秒だけ覚える)。衛星は全国で同じ電波なので、
  3拠点が同じ BS を見ていてもカードに聞くのは1回で済みます。**拠点が増えるほど効きます**

差し込み口は libaribb25 が用意してくれていました。カードは `B_CAS_CARD` という
**関数ポインタの表**で渡す作りなので、`proc_ecm` だけ別のところへ投げる表を作って
渡せば、復号の本体は何も変えずに済みます。

実機で確かめました。**カードを持っていない側として起こしたほうが、持っている側から
鍵を貰いながら 88.2% → 0.1% まで解けます。**

```sh
denpa-agent --tune /dev/dvb/adapter1/frontend0 T27 --decode --card http://拠点A:25252
```

**配る側は denpa ではなくエージェントです。** リーダーは USB の機材で、機材に
触るのはエージェント、という境界のままにしたいのが1つ。もう1つは、denpa (bun) から
PC/SC を触るには結局ネイティブの口を足すことになり、**既にカードを読めるものが
あるのに二重に持つ**ことになるからです。カードだけ挿さっていてチューナーが1本も
無いマシンでも、エージェントを1つ置けばそれが配り役になります。

**拠点にカードがあるならそれを使います。** そのほうが速く、配り役が落ちても
録れます。`--card` を指定しなければ手元のカードを読むので、**既定は今までどおり**です。

なお B-CAS カードの貸与条件は受信機ごとなので、**世帯をまたいで配るのは
グレー**です。手元の拠点をまとめる範囲で使うものと考えています。

#### カードを denpa 側に持たせない理由

**「1枚のカードで何本ものチューナーを解く」は、いまも既にそうなっています。**
カードを掴むのは pcscd で、pcscd は口を1つずつ順番に貸します。チューナーが4本
あっても、カードに用があるのは ECM が変わるとき (数秒に1回・数十ms) だけなので、
1枚で足ります。**カードが1枚だからエージェントに置けない、ということはありません。**

そのうえで、解くのをエージェント側に置く理由は3つあります。

1. **カードはあちらにしかありません。** リーダーは USB で、機材のあるマシンに
   挿さっています。denpa 側に持たせると、リーダーを denpa の側へ通すことになり、
   **エージェントを別のマシンに増やす**という話 (下記) が成り立たなくなります
2. **解くのは選局ごとに1回で足ります。** 相乗りを捌いているのはエージェントで、
   同じチャンネルを録画・番組表・ロゴの3人が読んでも選局は1本です。denpa 側で
   解くと、**送られてきた3本をそれぞれ解く**ことになり、MULTI2 が3倍になります
3. **denpa が中身を触らずに済みます。** 録るものをそのまま書くだけでよく、
   「解けているか」を気にするのは受け取ったあとの検査だけで済みます

エージェントを増やしてカードが片方にしか無いときに要るのも「カードごと移す」こと
ではなく **ECM を投げて鍵を貰う口**で、それが上の `CardShare.cs` です。

### エージェントは .NET (Native AOT)

**境界はもう引いてあります** ([冒頭](#チューナーエージェント)のとおり、
エージェントは TS を1バイトも解釈しません)。

| | 誰が | 何を |
| --- | --- | --- |
| **エージェント** | 機材に触るところ | デバイスを開く・選局する・取り合いを裁く・B25 を解く → **バイト列を配る** |
| **denpa (TypeScript)** | 中身を分かるところ | PSI/SI・EIT・ロゴ・番組表・予約・録画・画面 |

**この境界のほうが言語より大事です。** ここを崩してエージェントに EIT の解析まで
持たせると、どちらの言語を選んでも同じものを2箇所に持つことになります。

そのうえで、**掴んだままにするには ioctl を直に叩く必要がある**ので、
bun では書けませんでした。以下は選んだときの理由で、そのまま入っています。

#### なぜ .NET か

**手が動く言語だから**です。1人で持つものなので、録画に失敗した夜中に直せるかどうかが
いちばん効きます。そのうえで、この用途で .NET が困らないことを確かめました。

- **P/Invoke で ioctl も libaribb25 も呼べます。** `[LibraryImport]` と
  `[StructLayout(LayoutKind.Sequential)]`、そして C# の関数ポインタ
  (`delegate* unmanaged<...>`) で、`dtv_properties` も `struct ptx_freq` も素直に組めます
- **Native AOT で1個の実行ファイルになります。** 別PCへ置くのに「まず何かを入れて
  ください」が要らない、という条件はこれで満たせます。**AOT は必須**で、
  これを外すと別PCの話が崩れます
- **速さは問題になりません。** チューナー4本でも 10 MB/s 程度で、`Span<byte>` と
  `ArrayPool<byte>` を使えば十分に収まります

#### Rust と比べて失うもの

- **`b25-sys` が無い。** Rust には[実機で今まさに解いている](#b25-の解除は-libaribb25)
  wrapper がありますが、.NET には無いので自分で書きます。公開APIは関数ポインタの
  表だけなので**1日ぶんの写経**で済む見込みです (実際そのくらいでした。上記)
- **GC がある。** ただし気をつけるのは読み取りの一本道だけで、188バイトごとに
  確保しない (`ArrayPool` / `Span` / pipelines) を守れば足ります

#### 逆に、思ったほど失わないもの

**Rust の安全性の利点は、ここでは C 相手ほど大きくありません。** .NET も安全な言語で、
危ないのは**どちらも同じ FFI の境目だけ**です。Rust で書いても `unsafe` を通るのは
ioctl と `libaribb25` の呼び出しで、.NET の P/Invoke と同じ場所です。

#### 書き直しにはなりませんでした

移したのは取り合いの決まり・チャンネルの控え・HTTP の口だけです
(チャンネルスキャンは denpa へ移ったので、持って行くものがそもそも減りました)。
決まりは1つも変えていません。

移してみて出た違いは2つで、**どちらも適合テストが捕まえました。**

- **控えの並び順**。bun 版は種別ごとに並べ替えて書いていた
- **日本語の書き方**。`System.Text.Json` は既定で `\u30C6` に逃がすので、
  `channels.json` が人の目で読めなくなる。全角空白 (U+3000) だけは
  .NET のどの符号化器も逃がすので、そこは合わせていない

ついでに1つ直っています。**取り上げられた相手に失敗を伝えられるようになりました** —
bun では送信中の本文を打ち切っても正常終了として届いてしまい、取り上げられた録画が
尻切れのまま「録れた」ことになっていました。Kestrel は接続を壊せます
([上記](#選局は読み手が切るまで終わらない))。

**プロセスは分けます。** 「立てるものは denpa だけ」は*同じイメージに入っていて
denpa が面倒を見る*という意味で、**同じプロセスという意味ではありません**。
ioctl と復号は落ちるときに落ちるので、そこで画面や他の録画まで道連れにしません
(この判断は言語とは別で、どの言語で書いても同じ結論になります)。

### 通信は HTTP。ローカルは Unix ドメインソケット

**HTTP のところは既に入っています** (`/denpa/stream` が素のTSを chunked で流します)。
残っているのは Unix ドメインソケットで、いまは TCP だけです。

**先に言っておくと、遅延は伝送路にはありません。** 効いているのは選局の同期
(周波数を変えれば必ず要る) と、受け取った側のバッファです。ループバックや
Unix ソケットの往復は桁が3つ違います。**低遅延のためにやるべきことは「溜めないこと」**で、
どの protocol を選ぶかではありません。

そのうえで、いちばん退屈なものを選びます。

| | 何で | 中身 |
| --- | --- | --- |
| 制御 (選局・解放・状態) | HTTP + JSON | 小さい要求と応答 |
| 知らせ (掴んだ・離した・同期が外れた) | SSE | denpa は自分の画面でもう使っている |
| **TS** | HTTP の chunked | **素のTSだけ。何も包まない** |

**ローカルは Unix ドメインソケット、別PCは TCP。**同じ HTTP なので、denpa 側は
`/denpa/stream` を `fetch` して `response.body` を読む今の形のまま、**宛先が
変わるだけ**です。bun の `fetch` は `fetch(url, { unix })` でそのまま喋れます
(Linux では abstract socket も可)。.NET 側は Kestrel が Unix ソケットと TCP を
**同時に**listen できます。`curl --unix-socket … > x.ts` で人手でも確かめられます。

低遅延のために守ること:

- **溜めない。** 数パケット届いたらそのまま流します。64KB 貯めてから送ると、
  それだけで 25ms 積み上がります (1本 2.5 MB/s)
- **TCP を使うときは `TCP_NODELAY`。** 小さい書き込みが Nagle で待たされます
- **TSの上に枠を被せない。** 既に188バイトで区切られているものに、
  さらに長さやメッセージの枠を付ける理由がありません

採らないもの:

| 案 | なぜ |
| --- | --- |
| gRPC | .NET 側は良いが bun 側が重い。**不透明なバイト列に protobuf の枠は何も足しません** |
| HTTP/2 | 流しっぱなしの1本にフロー制御の窓が増えるだけ。h1 の chunked のほうが素直 |
| 共有メモリ / リングバッファ | いちばん速いが、**ローカル専用**なので道が2本になる。10 MB/s のために背負う複雑さではない |
| 自前のバイナリ protocol | 覗けなくなるだけ。枠が要らないことは上のとおり |

**背圧の向きは用途で変えます。** 録画は**落としません** (詰まったら待たせる。
電波は二度と来ません)。視聴は**落として構いません** (遅れて届くコマより、
いまのコマのほうが価値がある)。同じ選局を両方が読んでいることがあるので、
**読み手ごとに決める**必要があります。

### エージェントは denpa に同梱。増やすときだけ足す

いまは2つのイメージ (`denpa` と `denpa-agent`) を並べています。

- **既定では denpa と同じイメージに入っていて、denpa が起こします。** チューナーが
  1台のマシンに挿さっているふつうの構成では、**設置するものは denpa だけ**。
  ただし**プロセスは分けます** ([上記](#エージェントは-net-native-aot))
- **別PCのチューナーを足したいときは、そのPCにエージェントだけ入れる。**
  denpa の設定に住所を足すと、チューナーが増えたものとして扱う
- 取り合いは**denpa が全体を見て決め**、エージェントは言われたチャンネルを
  掴んで流す。どのエージェントの何本目か、は denpa 側の帳簿になる

### 気をつけること

- **録画を落とさない。** ここまでの入れ替えは選局の仕方を変えずに周りだけ
  動かしたので、壊れても番組表が古くなるだけでした。ここから先は
  **選局そのもの**を書き換えるので、失敗の出かたが変わります
- **答え合わせの相手が居ない。** 読んだ局や番組表を突き合わせられる相手が
  もう無く、実機で出た結果が唯一の正解です
  (エージェント同士は `agent/conformance.test.ts` で突き合わせられます)
- **[ライブ視聴](stream.md) と同じ土台**です。視聴・録画・番組表・ロゴが同じ
  取り合いの上に乗るので、設計はまとめて考えます

## B-CASカードとデスクランブル

`recisdb` は**既定で ARIB STD-B25 を復号します**(`--no-decode` で無効化)。ただし
**カードが開けないときは黙って復号せずに素通しします**。録画は成功してサイズもそれらしい
のに中身が全部スクランブル、という分かりにくい壊れ方をします。ログには音声フィルタの
エラーしか出ないので原因が見えません。

- エージェントが **`pcscd` を先に起動**します (ベースイメージに入っているが起動されない)
- **録画は止めません。** 電波は二度と戻ってこないので、スクランブルされたままでも
  録っておきます。denpa がエンコードの前に見て、掛かったままならそこで解きます
  (`src/lib/server/scramble.ts`)
- **解くのはチューナー側のエージェントです。** カードは pcscd 経由でしか読めず、
  その pcscd は向こうのコンテナにしか居ません。socket を共有して denpa から
  直接読ませていた頃は、カードを開けないままでした
- やり取りするのは**パスだけ**です。生TSの置き場 (`denpa-recorded`) を両方のコンテナに
  見せてあるので、読むのも書くのも向こうが直接やります。数十GBを HTTP で往復させません
  (**両方の Pod が同じノードに居ることが前提**です)
- **生TSを残す設定のときは、残るのは解除済みのTSだけ**です。掛かったままのものを
  取っておいても、あとから解ける保証はありません
- 状態は**チューナー画面の「つながり具合」**に出ます (エージェントのチューナー本数と
  カードリーダーが見えているか)

### 直らないとき

`pcscd` が動いていてもリーダーが見つからないことがあります。`pcscd -f -d` を前面で
走らせるとどこで止まっているか分かります。

```text
hotplug_libudev.c:421:HPAddDevice() Adding USB device: Gemalto PC Twin Reader
ccid_usb.c:899:WriteUSB() write failed (4/3): LIBUSB_ERROR_TIMEOUT
```

**デバイスは見つかるが最初の書き込みがタイムアウトする**場合、リーダーが掴まれたまま
固まっています。USB レベルで入れ直すと戻ります(`4-11` は `lsusb` と
`/sys/bus/usb/devices` から引く)。

```sh
kubectl -n denpa exec deploy/tuner-agent -- pkill pcscd
echo 4-11 | sudo tee /sys/bus/usb/drivers/usb/unbind
sleep 2
echo 4-11 | sudo tee /sys/bus/usb/drivers/usb/bind
kubectl -n denpa exec deploy/tuner-agent -- bash -c 'pcscd; sleep 2; pcsc_scan -r'
```

確認は次の3つ。リーダーが見えるか、カードが読めるか(B-CAS なら ATR が返る)、
実際に復号できているか(0% なら正常、壊れていると 98〜99%)。

```sh
kubectl -n denpa exec deploy/tuner-agent -- pcsc_scan -r
kubectl -n denpa exec deploy/tuner-agent -- bash -c 'timeout 8 pcsc_scan -n | head -12'
kubectl -n denpa exec deploy/tuner-agent -- bash -c '
  curl -s --max-time 10 "http://localhost:40772/api/services/<id>/stream?decode=1" > /tmp/s.ts
  perl -e "binmode STDIN; my (\$t,\$s)=(0,0);
    while (read(STDIN,\$b,188)==188) { last if substr(\$b,0,1) ne \"\x47\";
      \$t++; \$s++ if (ord(substr(\$b,3,1)) & 0xC0); }
    printf(\"packets=%d scrambled=%d (%.1f%%)\n\", \$t, \$s, \$t ? 100*\$s/\$t : 0);" < /tmp/s.ts'
```
