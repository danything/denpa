/**
 * 生TSを開くときの探り (probe) の長さ。
 *
 * チャンネル切り替え直後は前番組の PAT/PMT の残骸が先頭に混ざるため、長めに
 * 探ってから構成を確定させる。生TSを読む ffmpeg の口 (本番の焼き・CM の切り出し・
 * 字幕の絵出し) で同じ値を使う。ここ1つにしてあるのは、片方だけ変えて挙動が
 * 割れないようにするため
 */
export const TS_PROBE = ['-analyzeduration', '15000000', '-probesize', '30000000'] as const;
